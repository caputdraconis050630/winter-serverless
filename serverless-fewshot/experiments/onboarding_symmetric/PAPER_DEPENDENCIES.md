# Submission dependencies after the symmetric follow-up

The current paper is not automatically updated by this experiment. The prior
TTL accounting issue remains a separate correctness dependency. This map was
prepared before inspecting the new evaluation outcomes.

| Current paper location | Dependency | Required action before submission |
|---|---|---|
| `main.tex` abstract, `sections/introduction.tex`, `sections/discussion.tex` | 32-46% reduction, 1.66-1.78 WM premium, source-information interpretation | Replace with audited versioned endpoints and retain strong-simple counterevidence; do not transfer component effects to gate |
| `sections/onboarding.tex`, generated onboarding rows, onboarding figures | Historical three-seed DES and original TTL semantics | Regenerate from corrected replay; distinguish native policy from controller-calibrated v2; include strict start, actual WM, drain and uncertainty |
| `sections/design.tex` | Fixed q/TTL and gate specification | A calibrated controller is a new explicitly specified policy wrapper; disclose validation choice and do not portray all 180 candidates as one policy |
| `sections/methodology.tex`, simulator supplement | RNG coupling and TTL deadlines | Describe common event tapes, prospective expiration, execution/idle/init accounting and terminal scenario |
| `sections/handoff.tex`, 1,755-function 48h table, abstract 3.369 cold/fn | Original dynamic-TTL lifecycle replay | Audit and rerun its own loop; four-hour v2 cannot certify the 720-minute handoff or its WM/cost |
| `sections/handoff.tex`, steady/current-provider tables and Pareto figure | Separate active-pool update loop and original simulation | Separate corrected replay required for retained numerical claims, including apparently favorable or unfavorable results |
| `sections/validation.tex` live table | Measured live values versus simulation columns | Keep measured values distinct; inspect simulator provenance for predicted columns; do not change actual measurements based on DES correction |
| Overhead measurements | Shared predictor computation versus provisioning controller | Not automatically invalidated by TTL accounting; avoid system-total-cost claims and state the calibrated wrapper overhead if material |
| Drift-pending material and R26 outputs | Event-conditioned trigger, old simulator path, different reset/refit schedule | Not an unrestricted causal detector evaluation; disclose event knowledge or implement/revalidate causal behavior in a new campaign |
| Highlights, supplementary, artifact documentation | Same method names and result provenance | Synchronize only after final scientific verdict; do not hide adverse results by relegating them without discussion |

If the symmetric comparison is favorable, the supported claim concerns the
tested source-assisted forecasting and control configuration, not necessarily
meta-learning superiority over supervised learning. If adverse or inconclusive,
the original strong information-value claim cannot simply be restored.

Even a favorable four-hour result does not resolve the remaining rows. A reduced
paper must remove unsupported central claims and their downstream references;
moving unverified numbers to a supplement is not a correction. No automatic
submission, upload, or replacement zip is performed by this study.

The read-only R20 input audit is in `48H_REPLAY_READINESS.md`. Cached 1755-function
48h schedules and their trace alignment are available, but their first 4h
actions differ from the current onboarding cache even after the pool cap.
Separate numerical/update-path provenance remains necessary in addition to
corrected TTL accounting.
